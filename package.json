{
  "name": "vue-pdf-live-render",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "A measurement-driven, live-preview pagination engine for Vue + Tailwind documents (invoices, offers, delivery notes). Framework-agnostic core + Vue binding. Same DOM drives the on-screen preview and the Puppeteer printToPDF output.",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc -b --noEmit"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@vitejs/plugin-vue": "^5.2.1",
    "puppeteer": "^25.1.0",
    "tailwindcss": "^4.0.0",
    "typescript": "~5.6.3",
    "vite": "^6.0.7",
    "vue-tsc": "^2.2.0"
  }
}
